from setuptools import setup

setup(
    name='data_structures',
    version='',
    packages=find_packages(where='src'),
    package_dir={'':'src'},
    include_pacakge_data=TRUE,
    install_Requies=['pytest',]
    author='kwilson',
)
